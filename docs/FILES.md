# Файлы для нового проекта

Этот документ описывает структуру файлов, которые должны быть зафиксированы в репозитории для создания нового проекта.

## 📁 Структура файлов для Git

### Корневая директория

```
├── .gitignore              # Правила игнорирования файлов
└── docs/                   # Документация
    ├── README.md          # Основная документация
    ├── QUICK_START.md     # Быстрый старт
    ├── FILES.md           # Этот файл
    ├── AGENTS.md          # Документация для AI
    └── TROUBLESHOOTING.md # Решение проблем
```

### Инфраструктура (infra/)

```
infra/
├── docker-compose.yml     # Конфигурация Docker Compose
├── .env.example          # Пример конфигурации окружения
├── README.md             # Документация по инфраструктуре
├── init.sh               # Скрипт инициализации (Linux/Mac)
├── init.bat              # Скрипт инициализации (Windows)
└── fix-permissions.sh    # Скрипт исправления прав доступа
```

### Backend (backend/) - только инфраструктурные файлы

```
backend/
├── Dockerfile            # Docker образ для Laravel
├── docker-entrypoint.sh # Скрипт авто-инициализации Laravel
├── .dockerignore        # Исключения для Docker build
├── .gitkeep            # Пустой файл для сохранения директории в Git
└── .gitlab-ci.yml      # CI/CD конфигурация для Backend
```

**НЕ включаются** (создаются автоматически):
- `app/`, `config/`, `database/`, `routes/` - код Laravel
- `vendor/` - зависимости Composer
- `composer.json`, `composer.lock` - создаются Laravel
- `phpstan.neon`, `pint.json` - создаются entrypoint скриптом
- Все остальные файлы Laravel проекта

### Frontend (frontend/) - только инфраструктурные файлы

```
frontend/
├── Dockerfile            # Docker образ для Nuxt
├── docker-entrypoint.sh # Скрипт авто-инициализации Nuxt
├── .dockerignore        # Исключения для Docker build
├── .gitkeep            # Пустой файл для сохранения директории в Git
└── .gitlab-ci.yml      # CI/CD конфигурация для Frontend
```

**НЕ включаются** (создаются автоматически):
- `app/` - код Nuxt приложения
- `node_modules/` - зависимости npm
- `package.json`, `package-lock.json` - создаются Nuxt CLI
- `nuxt.config.ts`, `tsconfig.json` - создаются Nuxt CLI
- Все остальные файлы Nuxt проекта

## 🚫 Файлы, которые НЕ должны быть в Git

### Автоматически генерируемые:
- `node_modules/` - зависимости npm
- `vendor/` - зависимости Composer
- `.nuxt/` - кэш Nuxt
- `.phpstan-cache/` - кэш PHPStan
- `dist/`, `build/` - артефакты сборки

### Конфигурационные (содержат секреты):
- `infra/.env` - переменные окружения (используйте `.env.example`)
- `backend/.env` - переменные окружения Laravel

### IDE и системные:
- `.idea/`, `.vscode/` - настройки IDE
- `.DS_Store`, `Thumbs.db` - системные файлы

## ✅ Проверка перед коммитом

Убедитесь, что в репозитории только:

1. ✅ Инфраструктурные файлы (Dockerfile, docker-entrypoint.sh)
2. ✅ Конфигурация Docker Compose
3. ✅ Скрипты инициализации (в infra/)
4. ✅ Документация (в docs/)
5. ✅ CI/CD конфигурации (в backend/ и frontend/)
6. ✅ `.gitignore` правильно настроен

**НЕ коммитьте:**
- ❌ Код приложений (Laravel/Nuxt проекты создаются автоматически)
- ❌ Зависимости (node_modules, vendor)
- ❌ Секреты (.env файлы)
- ❌ Кэш и артефакты сборки

## 📝 Команды для проверки

```bash
# Проверить, что будет зафиксировано
git status

# Проверить структуру
find . -type f -name "Dockerfile" -o -name "docker-entrypoint.sh" -o -name "docker-compose.yml" | sort

# Убедиться, что зависимости не включены
git check-ignore node_modules vendor .nuxt
```

