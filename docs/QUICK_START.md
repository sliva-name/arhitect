# 🚀 Быстрый старт

## Шаг 1: Клонирование и подготовка

```bash
git clone <repository-url>
cd architect
```

## Шаг 2: Инициализация проекта

**Linux/Mac:**
```bash
cd infra
chmod +x init.sh
./init.sh
```

**Windows:**
```cmd
cd infra
init.bat
```

Скрипт автоматически:
- Создаст `.env` из `.env.example`
- Соберёт Docker образы
- Запустит все сервисы
- Инициализирует Laravel проект (если его нет)
- Инициализирует Nuxt проект (если его нет)

## Шаг 3: Доступ к приложениям

После завершения инициализации (1-3 минуты):

- **Frontend**: http://localhost:8080
- **Backend API**: http://api.localhost:8080  
- **Traefik Dashboard**: http://localhost:8081

## Шаг 4: Проверка работы

```bash
cd infra
docker-compose ps  # Проверить статус контейнеров
docker-compose logs backend  # Логи бэкенда
docker-compose logs frontend  # Логи фронтенда
```

## Что дальше?

- Смотрите [README.md](README.md) для подробной документации
- Смотрите [FILES.md](FILES.md) для понимания структуры проекта
- Смотрите [TROUBLESHOOTING.md](TROUBLESHOOTING.md) при возникновении проблем
